---
- name: Configure all servers
  hosts: all
  gather_facts: true
  become: true
  become_method: sudo
  serial: 10%
  max_fail_percentage: 40

  # vars:
  #   # Keep role names as variables to make the play easier to refactor later
  #   common_role_name: common
  #   docker_role_name: docker

  roles:
    - role: common
      tags:
        - common
    
    - role: docker
      tags:
        - docker

    # - role: k3s
    #   tags:
    #     - k3s

    # - role: metallb
    #   tags:
    #     - metallb

    # - role: adguard
    #   tags:
    #     - adguard

    # - role: plex
    #   tags:
    #     - plex

    # - role: qbittorrent
    #   tags:
    #     - qbittorrent

# - name: Configure K3s
#   hosts: k3s
#   become: true

#   roles:
#     - k3s
